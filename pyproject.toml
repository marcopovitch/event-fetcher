[project]
name = "event-fetcher"
version = "1.3.0"
description = "A project to fetch events"
authors = [
    { name = "Marc Grunberg", email = "marc.grunberg@unistra.fr" }
]
dependencies = [
    "icecream>=2.1.5",
    "obspy >= 1.4.0",
    "pandas>=2.3.1",
    "pyyaml",
    "seisbench>=0.9.1",
]

[tool.uv]
dev-dependencies = [
    "ipython",
    "icecream"
]
